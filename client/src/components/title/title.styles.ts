import { stylesheet } from 'typestyle'

export const css = stylesheet({
  title: {
    fontSize: '1rem',
    paddingBottom: '0.5rem',
    fontWeight: 500,
  },
  centered: {
    width: '100%',
    textAlign: 'center',
  },
})
